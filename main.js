/*

  first commit: the following is the first uploaded variant
  ! Note: do not attempt to load twice unless it fails to preload
  
*/

// sidebar

container = {
  sidebar: {
    template (width, height, instance){
    
    }
  }
}
